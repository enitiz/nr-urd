<%= provide(:title, 'Sign in') %>

<%= render 'layouts/hero_unit' %>

<div class="row-fluid">
	<div class="span4">
		<p></p>
	</div>
	<div class="span4">
		<h3 class="center">Sign in</h2>
		<%= form_for(resource, html: { class: "well center" }, :as => resource_name, :url => session_path(resource_name)) do |f| %>
			<%= f.email_field :email, placeholder: 'Email Address' %>
			<br />
			<%= f.password_field :password, placeholder: 'Password' %>
			<br />
			<% if devise_mapping.rememberable? -%>
				<%= f.check_box :remember_me %> Remember Me
			<% end -%>
			<br /> <br />
			<%= f.submit "Sign in to URD", class: "btn btn-large btn-success" %>
		<% end %>
	</div>
</div>
<%= render "links" %>
